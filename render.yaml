services:
  - type: web
    name: spanish-project
    env: ruby
    buildCommand: bundle install --bundler-version 2.4.22
    startCommand: bundle exec rails server
    plan: free
    runtime: ruby-3.2
    autoDeploy: true
